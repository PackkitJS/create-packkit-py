// Standalone so generate.ts and generator.ts share these without an import cycle.

/** This generator's stable platform id. */
export const GENERATOR_ID = 'python';

/** Provenance/definition schema version for generated Python projects. */
export const PROVENANCE_SCHEMA_VERSION = 1;
